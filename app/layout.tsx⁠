import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { PiProvider } from "@/components/providers/PiProvider";
import Script from "next/script";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "πPrime AI - Pi Ecosystem",
  description: "AI-Powered Promotion Platform for Pi Ecosystem",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        {/* تم تحديث الرابط إلى الرابط الرسمي للـ SDK لضمان التوافق مع متصفح Pi[span_1](start_span)[span_1](end_span) */}
        {/* استخدام strategy="beforeInteractive" يضمن تحميل المكتبة قبل بدء تشغيل تطبيق React[span_2](start_span)[span_2](end_span) */}
        <Script 
          src="https://sdk.pi.network/v1/pi.js" 
          strategy="beforeInteractive" 
        />
      </head>
      <body className={inter.className}>
        <PiProvider>
          {children}
        </PiProvider>
      </body>
    </html>
  );
}
